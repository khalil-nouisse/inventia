import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import HackathonCard from '../components/HackathonCard'; import { test, expect } from 'vitest'; test('renders hackathon title',()=>{render(<MemoryRouter><HackathonCard hackathon={{id:1,title:'ENSAM InventIA Challenge',theme:'Inventory',status:'UPCOMING'}}/></MemoryRouter>); expect(screen.getByRole('heading', { name: 'ENSAM InventIA Challenge' })).toBeInTheDocument();});
