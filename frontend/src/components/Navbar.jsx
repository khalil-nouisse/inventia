import { Link } from 'react-router-dom'; import { Button } from 'antd'; import { LogOut, Shield, Trophy, Moon, Sun } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import { useTheme } from '../context/ThemeContext';
export default function Navbar(){ const { user, logout, hasRole } = useAuth(); const { isDark, toggleTheme } = useTheme(); return <header className="topbar"><Link to="/" className="brand"><img src="/assets/logo-dark.png" alt="InventIA" className="logo" /></Link><nav><Button type="text" icon={isDark ? <Sun size={16}/> : <Moon size={16}/>} onClick={toggleTheme} style={{ color: 'var(--text-main)' }} /><Link to="/hackathons">Hackathons</Link>{hasRole?.(['ROLE_MANAGER','ROLE_ADMIN']) && <Link to="/dashboard"><Trophy size={16}/>Dashboard</Link>}{hasRole?.(['ROLE_ADMIN']) && <Link to="/admin/users"><Shield size={16}/>Users</Link>}{user ? <Button icon={<LogOut size={16}/>} onClick={logout}>Logout</Button> : <><Link to="/login">Login</Link><Link to="/register"><Button type="primary">Register</Button></Link></>}</nav></header> }
