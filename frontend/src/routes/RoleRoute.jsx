import { Navigate, Outlet } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'; export default function RoleRoute({ roles }){ const { hasRole } = useAuth(); return hasRole(roles) ? <Outlet /> : <Navigate to="/unauthorized" replace />; }