import { api } from './api'; export const hackathonService = { list: (p) => api.get('/hackathons', { params: p }), get: (id) => api.get(`/hackathons/${id}`), create: (p) => api.post('/hackathons', p), update: (id, p) => api.put(`/hackathons/${id}`, p), delete: (id) => api.delete(`/hackathons/${id}`), analytics: (id) => api.get(`/hackathons/${id}/analytics`), announce: (id) => api.post(`/hackathons/${id}/announce`), leaderboard: (id) => api.get(`/hackathons/${id}/leaderboard`), scoringSubmissions: (id) => api.get(`/hackathons/${id}/scoring-submissions`), myTeam: (id) => api.get(`/hackathons/${id}/my-team`) };
